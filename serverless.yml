service: api-testing
provider:
  name: aws
  runtime: nodejs20.x
  region: ap-south-1



funcation:
  Lambdafunction:
    handler: handler.lambda_handler
    timeout: 30
    events:
      - https:
          path: /test
          method: GET
          cors: true
